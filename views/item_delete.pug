extends layout 

block content 
  h1 #{title}: #{item.name}

  p Are you sure you want to delete this item?
  form(method="POST")
    div.form-group 
      input#itemid.form-control(type='hidden', name='itemid', value=item._id )
    button(type="submit") Delete #{item.name}